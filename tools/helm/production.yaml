replicaCount: 2

image:
  repository: nginx
  tag: alpine

service:
  port: 80

ingress:
  enabled: true
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
